# Library Frontend

React + Vite frontend for the Library project.

Quick start:

1. From `Frontend` folder install dependencies:

```powershell
npm install
```

2. Start dev server:

```powershell
npm run dev
```

3. Ensure backend is running on http://localhost:8080 or set `VITE_API_BASE` in `.env`.
